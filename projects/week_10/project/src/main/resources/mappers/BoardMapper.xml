<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.rookies.mapper.BoardMapper">
    <select id="findAll" resultType="com.project.rookies.dto.BoardDto">
        SELECT * FROM tb_boards WHERE title LIKE '%${keyword}%' ORDER BY id DESC
    </select>
    <select id="findByWriter" resultType="com.project.rookies.dto.BoardDto">
        SELECT * FROM tb_boards WHERE writer = '${writer}' AND title LIKE '%${keyword}%' ORDER BY id DESC
    </select>
    <select id="findByWriterOrAdmin" resultType="com.project.rookies.dto.BoardDto">
        SELECT * FROM tb_boards WHERE (writer = '${writer}' OR writer = 'admin') AND title LIKE '%${keyword}%' ORDER BY id DESC
    </select>
    <select id="findById" resultType="com.project.rookies.dto.BoardDto">
        SELECT * FROM tb_boards WHERE id = ${id}
    </select>
    <select id="count" resultType="long"> SELECT count(*) FROM tb_boards </select>
    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_boards (title, content, writer, filename, filepath)
        VALUES ('${title}', '${content}', '${writer}', '${filename}', '${filepath}')
    </insert>
    <update id="update">
        UPDATE tb_boards
        SET title = #{title},
        content = #{content}
        WHERE id = #{id}
    </update>
    <delete id="delete">
        DELETE FROM tb_boards
        WHERE id = #{id}
    </delete>
</mapper>