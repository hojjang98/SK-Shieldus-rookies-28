<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.rookies.mapper.TicketMapper">

    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_tickets (subject, result_log, created_at)
        VALUES (#{subject}, #{resultLog}, NOW())
    </insert>

    <select id="findAll" resultType="com.project.rookies.dto.TicketDto">
        SELECT * FROM tb_tickets ORDER BY id DESC
    </select>

</mapper>